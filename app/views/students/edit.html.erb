<h1>Editing Student</h1>

<%= render 'form' %>
<%= form_for @student, :url => students_path, :html => { :multipart => true } do |form| %>
  <%= form.file_field :avatar %>
<% end %>
<%= link_to 'Show', @student %> |
<%= link_to 'Back', students_path %>
